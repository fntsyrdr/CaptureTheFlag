package scarcity.captuetheflag.Models;

public class CaptureZones {
    //this is where code will go to allow multiple capture zones. When doing this, the CaptureZone must be switched to instanced variables over static.
}
